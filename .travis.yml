language: node_js
node_js:
  - "9"
cache:
  yarn: true
  directories:
  - node_modules
  - $HOME/.yarn-cache
addons:
  apt:
    sources:
      - sourceline: 'deb https://dl.yarnpkg.com/debian/ stable main'
        key_url: 'https://dl.yarnpkg.com/debian/pubkey.gpg'
    packages:
      - yarn
script:
  - npm install
  - sed -i '' -e "s/{DOMAIN}/$auth0_domain/" src/Auth/auth0-variables.js
  - sed -i '' -e "s/{CLIENT_ID}/$auth0_client_id/" src/Auth/auth0-variables.js
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master
